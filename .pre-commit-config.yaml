repos:
  - repo: local
    hooks:
      - id: maia-bypass-import-check
        name: MAIA Bypass Import Check
        description: Scans for banned import patterns that bypass the Airlock Gateway
        entry: python3 scripts/check_bypass_imports.py --ci
        language: python
        types: [python]
        stages: [commit, push]
