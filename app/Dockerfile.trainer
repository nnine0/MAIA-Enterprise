FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

# Add training dependencies
RUN pip install torch transformers peft datasets

COPY . .

CMD ["python", "wait_for_trigger.py"]